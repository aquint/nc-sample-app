<%= form_for(user.memberships.
                          build(team_id: @team.id)) do |f| %>
  <div><%= hidden_field_tag :team_id, @team.id %></div>
  <%= f.submit "Add to team", class: "btn btn-primary" %>
<% end %>