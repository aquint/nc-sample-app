<%= form_for(user.memberships.find_by(user_id: user.id),
             html: { method: :delete }) do |f| %>
  <div><%= hidden_field_tag :team_id, team.id %></div>
  <div><%= hidden_field_tag :user_id, user.id %></div>
  <%= f.submit "Leave", class: "btn" %>
<% end %>